const mdLinks = require('../');


describe('mdLinks', () => {

    it('should...', () => {
        expect(typeof nombre de funcion arriba).toBe('function')
        console.log('FIX ME!');
    });
    it('return something', () => {
        const data =
    })
});